%% Elsevier CAS Template for MyST
%% Supports both single column (cas-sc) and double column (cas-dc) layouts

[# if options.columns == 'double' #]
\documentclass[a4paper,fleqn]{cas-dc}
[# else #]
\documentclass[a4paper,fleqn]{cas-sc}
[# endif #]

[# if options.citation_style == 'numbers' #]
\usepackage[numbers]{natbib}
[# else #]
\usepackage[authoryear,longnamesfirst]{natbib}
[# endif #]

[- IMPORTS -]

\begin{document}
\let\WriteBookmarks\relax
\def\floatpagepagefraction{1}
\def\textpagefraction{.001}

[# if doc.short_title #]
\shorttitle{[-doc.short_title-]}
[# endif #]

[# if doc.authors #]
\shortauthors{[# for author in doc.authors #][-author.surname-][# if not loop.last #], [# endif #][# endfor #]}
[# endif #]

\title[mode=title]{[-doc.title-]}
[# if doc.subtitle #]
\title[mode=sub]{[-doc.subtitle-]}
[# endif #]

[# if parts.title_note #]
\tnotemark[1]
\tnotetext[1]{[-parts.title_note-]}
[# endif #]

[# for author in doc.authors #]
\author[[- author.affiliations | join(",", "index") -]]{[-author.name-]}[#- if author.orcid or author.deceased -#][[#- endif -#][#- if author.orcid -#]orcid=[-author.orcid-][#- endif -#][#- if author.orcid and author.deceased -#],[#- endif -#][#- if author.deceased -#]deceased[#- endif -#][#- if author.orcid or author.deceased -#]][#- endif -#]
[# if author.corresponding #]
\cormark[[-loop.index-]]
[# endif #]
[# if author.note #]
\fnmark[[-loop.index + 100-]]
[# endif #]
[# if author.email #]
\ead{[-author.email-]}
[# endif #]
[# if author.url #]
\ead[url]{[-author.url-]}
[# endif #]
[# if author.twitter #]
\ead[twitter]{[-author.twitter-]}
[# endif #]
[# if author.roles #]
\credit{[-author.roles | join(", ") | replace("–", "--")-]}
[# endif #]
[# endfor #]

[# for affiliation in doc.affiliations #]
\affiliation[[-affiliation.index-]]{organization={[-affiliation.name-]}[# if affiliation.address #],
            addressline={[-affiliation.address-]}[# endif #][# if affiliation.city #],
            city={[-affiliation.city-]}[# endif #][# if affiliation.postal_code #],
            postcode={[-affiliation.postal_code-]}[# endif #][# if affiliation.state #],
            state={[-affiliation.state-]}[# endif #][# if affiliation.country #],
            country={[-affiliation.country-]}[# endif #]}
[# endfor #]

[# for author in doc.authors #]
[# if author.corresponding #]
\cortext[[-loop.index-]]{Corresponding author}
[# endif #]
[# if author.note #]
\fntext[[-loop.index + 100-]]{[-author.note-]}
[# endif #]
[# endfor #]

[# if doc.authors | selectattr("equal_contributor") | list | length > 1 #]
\nonumnote{These authors contributed equally to this work.}
[# endif #]

[# if parts.note #]
\nonumnote{[-parts.note-]}
[# endif #]

[# if parts.abstract #]
\begin{abstract}
[-parts.abstract-]
\end{abstract}
[# elif doc.abstract #]
\begin{abstract}
[-doc.abstract-]
\end{abstract}
[# endif #]

[# if parts.graphical_abstract #]
\begin{graphicalabstract}
[-parts.graphical_abstract-]
\end{graphicalabstract}
[# elif options.graphical_abstract #]
\begin{graphicalabstract}
\includegraphics{[-options.graphical_abstract-]}
\end{graphicalabstract}
[# endif #]

[# if parts.highlights #]
\begin{highlights}
[-parts.highlights-]
\end{highlights}
[# elif parts.keypoints #]
\begin{highlights}
\item [-parts.keypoints | trim | replace("\n\n", "\n\\item ")-]
\end{highlights}
[# endif #]

[# if doc.keywords #]
\begin{keywords}
[-doc.keywords | join(" \\sep ")-]
\end{keywords}
[# endif #]

\maketitle

[-CONTENT-]

[# if parts.appendix #]
\appendix
\section*{Appendix}
\renewcommand{\thesubsection}{\Alph{subsection}}
\setcounter{subsection}{0}
[-parts.appendix-]
[# endif #]

\printcredits

\bibliographystyle{cas-model2-names}
[# if doc.bibliography #]
\bibliography{[-doc.bibliography | join(", ")-]}
[# endif #]

[# if parts.biography #]
[-parts.biography-]
[# endif #]

\end{document}
