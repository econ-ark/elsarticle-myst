%% Elsevier CAS Template for MyST
%% Supports both single column (cas-sc) and double column (cas-dc) layouts

[# if options.columns == 'double' #]
\documentclass[a4paper,fleqn[# if options.longmktitle #],longmktitle[# endif #]]{cas-dc}
[# else #]
\documentclass[a4paper,fleqn[# if options.longmktitle #],longmktitle[# endif #]]{cas-sc}
[# endif #]

%% Unicode support for XeLaTeX
\usepackage{fontspec}

[# if options.citation_style == 'numbers' #]
\usepackage[numbers]{natbib}
[# else #]
\usepackage[authoryear,longnamesfirst]{natbib}
[# endif #]

%% Algorithm support
\usepackage{algorithm}
\usepackage{algpseudocode}

[- IMPORTS -]

%% Floats can only appear after their reference point, not before
\usepackage{flafter}

\begin{document}

%% Float placement: encourage floats to stay near their definition
%% Fractions control max float area per page; textfraction sets minimum text
%% floatpagefraction controls when LaTeX creates float-only pages
\renewcommand{\topfraction}{.75}
\renewcommand{\bottomfraction}{.7}
\renewcommand{\textfraction}{.15}
\renewcommand{\floatpagefraction}{.8}
\setcounter{topnumber}{4}
\setcounter{bottomnumber}{4}
\setcounter{totalnumber}{10}
\let\WriteBookmarks\relax

[# if doc.short_title #]
\shorttitle{[-doc.short_title-]}
[# else #]
\shorttitle{[-doc.title-]}
[# endif #]

%% Extract surname as last word from full name (handles middle initials correctly)
[# if doc.authors #]
\shortauthors{[# for author in doc.authors #][-author.name.split(" ") | last-][# if not loop.last #], [# endif #][# endfor #]}
[# endif #]

\title[mode=title]{[-doc.title-]}
[# if doc.subtitle #]
\title[mode=sub]{[-doc.subtitle-]}
[# endif #]

[# if parts.title_note #]
\tnotemark[1]
\tnotetext[1]{[-parts.title_note-]}
[# endif #]

%% Author metadata with optional attributes (ORCID, social links, etc.)
[# for author in doc.authors #]
[#- set author_opts = '' -#]
[#- if author.orcid -#][#- set author_opts = 'orcid=' ~ author.orcid -#][#- endif -#]
[#- if author.deceased -#][#- set author_opts = author_opts ~ (',' if author_opts else '') ~ 'deceased' -#][#- endif -#]
[#- if author.facebook -#][#- set author_opts = author_opts ~ (',' if author_opts else '') ~ 'facebook=' ~ author.facebook -#][#- endif -#]
[#- if author.linkedin -#][#- set author_opts = author_opts ~ (',' if author_opts else '') ~ 'linkedin=' ~ author.linkedin -#][#- endif -#]
\author[[- author.affiliations | join(",", "index") -]]{[-author.name-]}[# if author_opts #][[- author_opts -]][# endif #]
[# if author.corresponding #]
\cormark[[-loop.index-]]
[# endif #]
[# if author.note #]
\fnmark[[-loop.index + 100-]]
[# endif #]
[# if author.email #]
\ead{[-author.email-]}
[# endif #]
[# if author.url #]
\ead[url]{[-author.url-]}
[# endif #]
[# if author.twitter #]
\ead[twitter]{[-author.twitter-]}
[# endif #]
[# if author.roles #]
\credit{[-author.roles | join(", ")-]}
[# endif #]
[# endfor #]

%% op={} suppresses "missing address" warning when affiliation has only organization name
[# for affiliation in doc.affiliations #]
[#- set has_address = affiliation.department or affiliation.address or affiliation.city or affiliation.postal_code or affiliation.state or affiliation.country -#]
\affiliation[[-affiliation.index-]]{organization={[-affiliation.name-]}[# if not has_address #],op={}[# endif #][# if affiliation.department #],
            department={[-affiliation.department-]}[# endif #][# if affiliation.address #],
            addressline={[-affiliation.address-]}[# endif #][# if affiliation.city #],
            city={[-affiliation.city-]}[# endif #][# if affiliation.postal_code #],
            postcode={[-affiliation.postal_code-]}[# endif #][# if affiliation.state #],
            state={[-affiliation.state-]}[# endif #][# if affiliation.country #],
            country={[-affiliation.country-]}[# endif #]}
[# endfor #]

[# for author in doc.authors #]
[# if author.corresponding #]
\cortext[[-loop.index-]]{Corresponding author}
[# endif #]
[# if author.note #]
\fntext[[-loop.index + 100-]]{[-author.note-]}
[# endif #]
[# endfor #]

[# if doc.authors | selectattr("equal_contributor") | list | length > 1 #]
\nonumnote{These authors contributed equally to this work.}
[# endif #]

[# if parts.acknowledgements #]
\nonumnote{[-parts.acknowledgements-]}
[# endif #]

[# if parts.note #]
\nonumnote{[-parts.note-]}
[# endif #]

[# if parts.abstract #]
\begin{abstract}
[-parts.abstract-]
\end{abstract}
[# elif doc.abstract #]
\begin{abstract}
[-doc.abstract-]
\end{abstract}
[# endif #]

[# if parts.graphical_abstract #]
\begin{graphicalabstract}
[-parts.graphical_abstract-]
\end{graphicalabstract}
[# elif options.graphical_abstract #]
\begin{graphicalabstract}
\includegraphics{[-options.graphical_abstract-]}
\end{graphicalabstract}
[# endif #]

[# if parts.highlights #]
\begin{highlights}
[-parts.highlights-]
\end{highlights}
[# elif parts.keypoints #]
\begin{highlights}
\item [-parts.keypoints | trim | replace("\n\n", "\n") | replace("\n", "\n\\item ")-]
\end{highlights}
[# endif #]

[# if doc.keywords #]
\begin{keywords}
[-doc.keywords | join(" \\sep ")-]
\end{keywords}
[# endif #]

\maketitle

[-CONTENT-]

[# if parts.appendix #]
\appendix
\section*{Appendix}
\renewcommand{\thesubsection}{\Alph{subsection}}
\setcounter{subsection}{0}
[-parts.appendix-]
[# endif #]

\printcredits

\bibliographystyle{cas-model2-names}
[# if doc.bibliography #]
\bibliography{[-doc.bibliography | join(", ")-]}
[# endif #]

[# if parts.biography #]
[-parts.biography-]
[# endif #]

\end{document}
